<p id="notice"><%= notice %></p>


<div class="show">
<center>
  <b>
  <%= @product.name %></b>

<br><table><tr><td>
  <%= image_tag @product.image %></td><td>
 <% if @product.video != ""  %>
 <%=  raw @product.video %></td><tr><td colspan=2>
 <% end %>
 <center>
<p>
  <%= @product.description %>
</p>

<p>

  <%= number_to_currency @product.price %>
</p>

<p>
  <b>Only <%= @product.quantity %> left in stock!</b>
</p>
 <%= button_to 'Add to Cart', line_items_path(:product_id => @product) %></center>
</td>
</table>
<% if cookies[:vendid] == @secretword %>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Destroy', @product, method: :delete, data: { confirm: 'Are you sure?' } %> |
<% end %>
<%= link_to 'Back', products_path %>
</div>